# Dependencies
node_modules/

# Plugin cache (regenerated on install)
cache/

# Runtime codebase cache inside projects (scan-cache + search index)
**/.openclaude/codebase-cache/

# Credentials — never commit real API keys
**/context7-keys.json

# SciSpace session cookies — never commit real session data
**/storage-state.json
**/cookies.txt

# Downloaded binary (install via codebase-memory/install-binary.ps1)
codebase-memory/bin/*.exe

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp
